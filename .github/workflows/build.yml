name: Build MyPvP Client Dylib

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Tweak
    runs-on: macos-latest

    steps:
      - name: 📥 Projeyi Çek (Checkout)
        uses: actions/checkout@v4

      - name: 🔑 İmzalama Aracını Kur (ldid)
        run: brew install ldid

      - name: 🛠️ Theos'u Kur
        run: |
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: 📦 iOS SDK'larını Kur
        run: |
          curl -LO https://github.com/theos/sdks/archive/master.zip
          unzip master.zip
          mv sdks-master/* $HOME/theos/sdks/
          rm -rf sdks-master master.zip

      - name: ⚙️ Projeyi Derle (Build)
        run: |
          make package FINALPACKAGE=1
        env:
          THEOS: ${{ env.THEOS }}

      - name: 🚀 Sadece Dylib Dosyasını Kaydet
        uses: actions/upload-artifact@v4
        with:
          name: MyPvPClient-Dylib
          path: .theos/obj/**/*.dylib
